{
  "name": "tjkite",
  "version": "1.0.0",
  "private": true,
  "description": "TJKite kite school manager — local Node server + Cloudflare Pages deploy.",
  "scripts": {
    "dev": "node server.js",
    "sync": "node sync-public.js",
    "predev:cf": "npm run sync",
    "dev:cf": "wrangler pages dev",
    "predeploy": "npm run sync",
    "deploy": "wrangler pages deploy",
    "kv:create": "wrangler kv namespace create TJKITE_KV && wrangler kv namespace create TJKITE_KV --preview",
    "secret:set": "wrangler pages secret put TJKITE_PASSWORD"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
