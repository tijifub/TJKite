node_modules/
.wrangler/
.dev.vars
.DS_Store
tjkite-server.log

# Local-only state file — production state lives in Cloudflare KV.
shared-data.json

# Source import file (PII); deployed data lives in students-data.js behind auth.
students.csv

# Generated by sync-public.js — Cloudflare/Wrangler doesn't follow symlinks, so
# these are real-file copies of the root sources, regenerated before every
# dev:cf / deploy. Never edit these directly.
cloudflare/public/index.html
cloudflare/public/students-data.js
cloudflare/public/voucher-bg.jpg
